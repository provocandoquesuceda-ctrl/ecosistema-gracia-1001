'use client';

import { useState } from 'react';
import Link from 'next/link';

export default function CertificadosPage() {
  const [nombreAlumno, setNombreAlumno] = useState('Líder del Reino');
  const [certificadoSeleccionado, setCertificadoSeleccionado] = useState<number | null>(null);

  const niveles = [
    {
      id: 1,
      titulo: 'Certificado en Fundamentos de la Gracia e Identidad',
      trimestre: 'Trimestres 1 y 2',
      estado: 'Completado',
      fecha: '2026',
      codigo: 'GRACIA-1001-N1'
    },
    {
      id: 2,
      titulo: 'Certificado en Exégesis del Nuevo Pacto y Hermenéutica',
      trimestre: 'Trimestres 3 y 4',
      estado: 'Completado',
      fecha: '2026',
      codigo: 'GRACIA-1001-N2'
    },
    {
      id: 3,
      titulo: 'Especialista en Liderazgo Cristocéntrico y Salud Ministerial',
      trimestre: 'Trimestres 5 y 6',
      estado: 'En Progreso',
      fecha: 'En curso',
      codigo: 'GRACIA-1001-N3'
    },
    {
      id: 4,
      titulo: 'Licenciatura en Teología de la Gracia y la Verdad',
      trimestre: 'Programa Completo (8 Trimestres)',
      estado: 'Pendiente',
      fecha: 'Pendiente',
      codigo: 'GRACIA-1001-LIC'
    }
  ];

  return (
    <main className="min-h-screen bg-slate-900 text-white p-6 max-w-5xl mx-auto space-y-8">
      {/* Header */}
      <header className="border-b border-slate-800 pb-6 space-y-2">
        <span className="text-xs font-semibold uppercase tracking-wider text-amber-400 bg-amber-950/80 px-3 py-1 rounded-full border border-amber-800">
          🏅 Acreditación & Reconocimiento
        </span>
        <h1 className="text-3xl font-bold mt-2">Portal de Certificaciones Digitales</h1>
        <p className="text-slate-400 text-sm">
          Acredita tus avances académicos y ministeriales respaldados por la Universidad de la Gracia y la Verdad.
        </p>
      </header>

      {/* Input Nombre Alumno */}
      <div className="bg-slate-800 p-4 rounded-2xl border border-slate-700 flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4">
        <div>
          <label className="text-xs text-slate-400 block mb-1">Nombre para los Certificados:</label>
          <input
            type="text"
            value={nombreAlumno}
            onChange={(e) => setNombreAlumno(e.target.value)}
            className="bg-slate-900 border border-slate-700 text-white px-3 py-2 rounded-xl text-sm focus:outline-none focus:border-amber-500 w-64"
          />
        </div>
        <div className="text-xs text-amber-400 bg-amber-950/40 p-3 rounded-xl border border-amber-800/50">
          ℹ️ Los certificados cuentan con un código único de verificación en cadena.
        </div>
      </div>

      {/* Lista de Certificados */}
      <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
        {niveles.map((n) => (
          <div
            key={n.id}
            className="bg-slate-800 p-6 rounded-2xl border border-slate-700 flex flex-col justify-between space-y-4 shadow-lg"
          >
            <div className="space-y-2">
              <div className="flex justify-between items-center">
                <span className="text-[10px] uppercase font-bold text-slate-400 bg-slate-900 px-2 py-1 rounded border border-slate-700">
                  {n.trimestre}
                </span>
                <span
                  className={`text-[10px] font-bold px-2 py-1 rounded-full border ${
                    n.estado === 'Completado'
                      ? 'bg-emerald-950 text-emerald-400 border-emerald-800'
                      : n.estado === 'En Progreso'
                      ? 'bg-amber-950 text-amber-400 border-amber-800'
                      : 'bg-slate-900 text-slate-500 border-slate-700'
                  }`}
                >
                  {n.estado}
                </span>
              </div>
              <h3 className="font-bold text-slate-100 text-base leading-snug">{n.titulo}</h3>
              <p className="text-xs font-mono text-slate-400">Código ID: {n.codigo}</p>
            </div>

            {n.estado === 'Completado' ? (
              <button
                onClick={() => setCertificadoSeleccionado(n.id)}
                className="w-full bg-amber-600 hover:bg-amber-500 text-white font-bold text-xs py-2.5 rounded-xl transition-all shadow-md flex items-center justify-center gap-2"
              >
                <span>📜</span> Visualizar Certificado
              </button>
            ) : (
              <button
                disabled
                className="w-full bg-slate-700/50 text-slate-500 font-bold text-xs py-2.5 rounded-xl cursor-not-allowed"
              >
                Completa el ciclo para desbloquear
              </button>
            )}
          </div>
        ))}
      </div>

      {/* Modal / Previsualización de Certificado */}
      {certificadoSeleccionado !== null && (
        <div className="fixed inset-0 bg-black/80 backdrop-blur-sm flex items-center justify-center p-4 z-50">
          <div className="bg-slate-900 border-2 border-amber-500/80 p-8 rounded-3xl max-w-2xl w-full text-center space-y-6 shadow-2xl relative">
            <button
              onClick={() => setCertificadoSeleccionado(null)}
              className="absolute top-4 right-4 text-slate-400 hover:text-white text-sm"
            >
              ✕ Cerrar
            </button>

            <div className="border-4 border-double border-amber-500/40 p-6 rounded-2xl space-y-4">
              <span className="text-3xl">🏛️</span>
              <h2 className="text-xs uppercase tracking-widest text-amber-400 font-bold">
                Universidad de la Gracia y la Verdad
              </h2>
              <p className="text-xs text-slate-400 uppercase tracking-widest">Otorga el presente certificado a:</p>
              <h1 className="text-2xl font-extrabold text-amber-200 border-b border-amber-500/30 pb-2 inline-block">
                {nombreAlumno}
              </h1>
              <p className="text-xs text-slate-300 leading-relaxed max-w-md mx-auto">
                Por haber completado satisfactoriamente los requerimientos académicos del nivel:{' '}
                <strong className="text-amber-400">
                  {niveles.find((x) => x.id === certificadoSeleccionado)?.titulo}
                </strong>
              </p>
              <div className="pt-4 flex justify-between items-center text-[10px] text-slate-500 font-mono border-t border-slate-800">
                <span>Ecosistema Gracia 1001</span>
                <span>Firma de Verificación Digital</span>
              </div>
            </div>

            <button
              onClick={() => window.print()}
              className="bg-emerald-600 hover:bg-emerald-500 text-white font-bold text-xs px-6 py-2.5 rounded-xl transition-all shadow-lg"
            >
              🖨️ Imprimir / Guardar en PDF
            </button>
          </div>
        </div>
      )}
    </main>
  );
}
'use client';

import Link from 'next/link';

export default function CertificadosPage() {
  const certificados = [
    {
      id: 'CERT-2026-001',
      titulo: 'Certificado en Hermenéutica & Exégesis Cristocéntrica',
      nivel: 'Nivel 1 - Básico',
      fecha: '15 de Mayo, 2026',
      estado: 'Emitido',
      color: 'border-amber-500 text-amber-400 bg-amber-950/30'
    },
    {
      id: 'CERT-2026-042',
      titulo: 'Diplomado en Salud Pastoral & Cuidado Ministerial',
      nivel: 'Nivel 2 - Intermedio',
      fecha: 'Inscrito / En evaluación final',
      estado: 'En Proceso',
      color: 'border-cyan-500 text-cyan-400 bg-cyan-950/30'
    }
  ];

  return (
    <main className="min-h-screen bg-slate-900 text-white p-6 max-w-4xl mx-auto space-y-8">
      {/* Header */}
      <header className="border-b border-slate-800 pb-6 space-y-2">
        <span className="text-xs font-semibold uppercase tracking-wider text-amber-400 bg-amber-950/80 px-3 py-1 rounded-full border border-amber-800">
          🏅 Acreditación Académica
        </span>
        <h1 className="text-3xl font-bold mt-2">Mis Certificados & Titulaciones</h1>
        <p className="text-slate-400 text-sm">
          Acreditaciones oficiales emitidas por la Universidad de la Gracia y la Verdad.
        </p>
      </header>

      {/* Lista de Certificados */}
      <div className="space-y-4">
        {certificados.map((cert) => (
          <div
            key={cert.id}
            className="bg-slate-800/90 p-6 rounded-3xl border border-slate-700 flex flex-col md:flex-row justify-between items-start md:items-center gap-4 hover:border-slate-600 transition-all"
          >
            <div className="space-y-2">
              <div className="flex items-center gap-2">
                <span className={`text-[10px] uppercase font-bold px-2.5 py-0.5 rounded-full border ${cert.color}`}>
                  {cert.estado}
                </span>
                <span className="text-xs font-mono text-slate-500">{cert.id}</span>
              </div>
              <h2 className="text-base font-bold text-slate-100">{cert.titulo}</h2>
              <p className="text-xs text-slate-400">
                {cert.nivel} • <span className="text-slate-300">{cert.fecha}</span>
              </p>
            </div>

            <div className="w-full md:w-auto flex items-center gap-2">
              {cert.estado === 'Emitido' ? (
                <button
                  onClick={() => alert(`Descargando certificado oficial ${cert.id}...`)}
                  className="w-full md:w-auto bg-amber-500 hover:bg-amber-400 text-slate-950 font-bold text-xs px-4 py-2.5 rounded-xl transition-all flex items-center justify-center gap-2"
                >
                  <span>📜</span> Descargar Diploma PDF
                </button>
              ) : (
                <Link
                  href="/universidad/evaluacion"
                  className="w-full md:w-auto bg-slate-700 hover:bg-slate-600 text-slate-200 font-bold text-xs px-4 py-2.5 rounded-xl transition-all text-center block"
                >
                  Completar Evaluación →
                </Link>
              )}
            </div>
          </div>
        ))}
      </div>

      {/* Callout de Validación Oficial */}
      <div className="bg-slate-800/50 p-6 rounded-2xl border border-slate-700/80 flex items-center gap-4">
        <span className="text-3xl">🛡️</span>
        <div className="space-y-1">
          <h3 className="text-xs font-bold text-slate-200 uppercase tracking-wide">Validación de Firma Digital</h3>
          <p className="text-xs text-slate-400">
            Todos los certificados emitidos cuentan con verificación criptográfica y registro único en nuestro libro matriz de alumnos.
          </p>
        </div>
      </div>

      {/* Botón Volver */}
      <div className="pt-2">
        <Link
          href="/universidad"
          className="inline-block bg-slate-800 hover:bg-slate-700 text-amber-400 border border-slate-700 font-bold text-xs px-5 py-2.5 rounded-xl transition-all"
        >
          ← Volver a la Universidad
        </Link>
      </div>
    </main>
  );
}
